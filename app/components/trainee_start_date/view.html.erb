<%= render MappableSummary::View.new(
  trainee: trainee,
  title: t("components.confirmation.start_date.title"),
  rows: [{
      field_value: start_date,
      field_label: t("components.confirmation.start_date.title"),
      action_url: edit_trainee_start_date_path(trainee),
    }],
  editable: editable,
  has_errors: false
) %>
