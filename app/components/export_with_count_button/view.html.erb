
<% if count.zero? %>
  <%= govuk_inset_text do %>
    <%= no_record_text %>
  <% end %>
<% end %>

<%= govuk_button_to("#{button_text} (#{count} #{count_label.pluralize(count)})", href, method: :get) %>
