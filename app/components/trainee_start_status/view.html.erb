<%= render MappableSummary::View.new(
  trainee: trainee,
  title: t("components.confirmation.start_status.title"),
  rows: [{
      field_value: start_status_or_date,
      field_label: t("components.confirmation.start_status.title"),
      action_url: edit_trainee_start_status_path(trainee),
    }],
  editable: editable,
  has_errors: false
) %>
