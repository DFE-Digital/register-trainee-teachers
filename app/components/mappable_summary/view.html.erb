<%= render SummaryCard::View.new(
  title: title,
  rows: mappable_rows,
  id_suffix: id_suffix,
  heading_level: header_level,
) do |component|
  component.with_header_actions do %>
    <%= header_actions %>
  <% end
end %>
