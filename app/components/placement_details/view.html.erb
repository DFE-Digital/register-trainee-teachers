<%= render MappableSummary::View.new(
  trainee: trainee,
  title: summary_title,
  rows: rows,
  editable: false,
  has_errors: has_errors,
  ) do |component| %>
  <% component.header_actions do %>
    <% if editable %>
      <%= govuk_link_to "Manage placements", '#' %>
    <% end %>
  <% end %>
<% end %>
