<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds-from-desktop">
    <h1 class="govuk-heading-l">
      <span class="govuk-caption-xl"><%= organisation_name %></span>
      Upload Summary
    </h1>

    <% if @bulk_update_trainee_upload.trainee_upload_rows.with_errors.blank? %>
      <h2 class="govuk-heading-l">
        File validation passed
      </h2>
    <% end %>

    <p class="govuk-body">
      You uploaded a CSV file with details of <%= pluralize(upload.number_of_trainees, "trainee") %>.
    </p>

    <p class="govuk-body">
      It included:
    </p>

    <ul class="govuk-list govuk-list--bullet">
      <% if upload.trainee_upload_rows.without_errors.present? %>
        <li>
          <%= pluralize(upload.trainee_upload_rows.without_errors.size, "trainee") %> who can be added
        </li>
      <% end %>

      <% if upload.row_errors.duplicate.present? %>
        <li>
          <%= pluralize(upload.trainee_upload_rows.with_duplicate_errors.size, "trainee") %>
          who will not be added, as they already exist in Register
        </li>
      <% end %>

      <% if upload.row_errors.validation.present? %>
        <li>
          <%= pluralize(upload.trainee_upload_rows.with_validation_errors.size, "trainee") %>
          with errors in their details
        </li>
      <% end %>
    </ul>

    <p class="govuk-body">
      Do you want to add the trainees to Register?
    </p>

    <%= register_form_with(
      model: upload,
      url: bulk_update_add_trainees_submission_path(upload),
      method: :post,
      builder: GOVUKDesignSystemFormBuilder::FormBuilder,
    ) do |f| %>
      <%= f.govuk_submit("Submit") %>
    <% end %>
    <p class="govuk-body">
      <%= register_form_with(
        model: upload,
        url: bulk_update_add_trainees_upload_path(upload),
        method: :delete
      ) do |f| %>
        <%= f.submit "Cancel bulk updates to records", class: "govuk-link app-button--link govuk-body" %>
      <% end %>
    </p>
  </div>
</div>
