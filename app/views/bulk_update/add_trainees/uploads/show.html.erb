<%= render PageTitle::View.new(text: "Upload summary") %>

<%= content_for(:breadcrumbs) do %>
  <%= render GovukComponent::BackLinkComponent.new(
    text: "Back",
    href: [
      request.url,
      nil,
    ].include?(request.referrer) ? new_bulk_update_add_trainees_upload_path : request.referrer
  ) %>
<% end %>

<%= render(
 "BulkUpdate::TraineeUploads::Show::#{@bulk_update_trainee_upload.status.camelcase}::View".constantize.new(
   upload: @bulk_update_trainee_upload,
   current_user: current_user,
   bulk_add_trainee_upload_form: bulk_add_trainee_upload_form,
 ),
) %>
