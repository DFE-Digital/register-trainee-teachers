<p class="govuk-body">
  It included:
</p>

<ul class="govuk-list govuk-list--bullet">
  <% if awardable_rows_count.positive? %>
    <li>
      <%= pluralize(awardable_rows_count, "trainee") %> who will gain <%= qts_or_eyts(recommendations_upload.awardable_rows) %>
    </li>
  <% end %>

  <% if missing_date_rows_count.positive? %>
    <li>
      <%= pluralize(missing_date_rows_count, "trainee") %>  who will not change status, as you did not give a date when they gained
      <%= qts_or_eyts(recommendations_upload.awardable_rows) %> 
    </li>
  <% end %>

  <% if error_rows_count.positive? %>
    <li>
      <%= pluralize(error_rows_count, "trainee") %> with errors
    </li>
  <% end %>
</ul>

<% if error_rows_count.positive? %>
  <p class="govuk-body">
    You need to review the errors before you can bulk change trainee statuses.
  </p>

  <%= govuk_link_to("Review errors", bulk_update_recommendations_upload_recommendations_errors_path(recommendations_upload), class: "govuk-button") %>
<% else %>
  <%= govuk_link_to("Check whose status has changed", bulk_update_recommendations_upload_recommendations_checks_path(recommendations_upload), class: "govuk-button") %>
<% end %>
