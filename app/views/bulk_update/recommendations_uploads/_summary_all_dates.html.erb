<p class="govuk-body">
  <%= case awardable_rows_count
    when 1
      "The trainee"
    when 2
      "Both trainees"
    else
      "All #{awardable_rows_count} trainees"
    end %> who hold <%= qts_or_eyts(recommendations_upload.awardable_rows) %> selected.
</p>

<%= govuk_link_to(
  "Check who you've selected",
  bulk_update_recommendations_upload_recommendations_checks_path(recommendations_upload),
  class: "govuk-button"
  ) %>
