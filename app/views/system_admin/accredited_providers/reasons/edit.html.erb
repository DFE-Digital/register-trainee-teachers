<div class="govuk-form-group">
  <label class="govuk-label"><%= @trainee.full_name %></label>

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds-from-desktop">
      <%= register_form_with(model: @trainee, url: trainee_accredited_providers_reason_path(trainee_id: @trainee.id)) do |f| %>

        <%= f.govuk_error_summary %>

        <h1 class="govuk-heading-l">Why you’re changing the accredited provider</h1>

        <p>
          <%=
            f.govuk_text_field(
              :timeline_comment,
              hint: { text: "For example, ‘Provider no longer able to continue with this trainee’" },
              width: "two-thirds",
              label: { text: "Comment for trainee timeline", size: "m" },
              autocomplete: :off,
            )
          %>
        </p>

        <p>
          <%=
            f.govuk_text_field(
              :zendesk_ticket_url,
              hint: { text: "For example, ‘https://becomingateacher.zendesk.com/agent/tickets/12345’" },
              width: "two-thirds",
              label: { text: "Zendesk ticket URL (optional)", size: "m" },
              autocomplete: :off,
            )
          %>
        </p>

        <%= f.govuk_submit %>
      <% end %>
    </div>
  </div>
</div>

