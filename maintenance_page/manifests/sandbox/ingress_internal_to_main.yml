apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: register-sandbox.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: register-sandbox.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: register-sandbox
            port:
              number: 80
